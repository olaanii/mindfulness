import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets(
    'App smoke test requires Firebase — skipped until integration test harness',
    (WidgetTester tester) async {},
    skip: true,
  );
}
